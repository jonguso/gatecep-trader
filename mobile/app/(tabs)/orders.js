import { useEffect,useState } from "react";import { ScrollView,Text,StyleSheet } from "react-native";import API from "../../src/api";import { Page,Header,Card,ActivityRow } from "../../src/components/ProTradingUI";import { P } from "../../src/theme/proTheme";
export default function Orders(){const[orders,setOrders]=useState([]);useEffect(()=>{API.get("/orders?userId=u1").then(r=>setOrders(r.data||[])).catch(()=>{})},[]);return <Page><Header title="Orders"/><ScrollView><Card><Text style={s.section}>Today&apos;s Orders</Text>{orders.length===0&&<Text style={s.empty}>No orders today</Text>}{orders.map(o=><ActivityRow key={o.id} item={o}/>)}</Card></ScrollView></Page>};const s=StyleSheet.create({section:{color:P.color.text,fontSize:18,fontWeight:"900",marginBottom:8},empty:{color:P.color.muted}});
