/**
 * Legacy compatibility wrapper.
 * Source moved to src/services/calendar/calendarHubData.js.
 * Keep this wrapper until Expo Router app imports are fully migrated.
 */
export * from "../services/calendar/calendarHubData.js";
