/**
 * Legacy compatibility wrapper.
 * Source moved to src/services/coach/recommendationLifecycleStore.js.
 * Keep this wrapper until Expo Router app imports are fully migrated.
 */
export * from "../services/coach/recommendationLifecycleStore.js";
