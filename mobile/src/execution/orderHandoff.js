/**
 * Legacy compatibility wrapper.
 * Source moved to src/services/execution/orderHandoff.js.
 * Keep this wrapper until Expo Router app imports are fully migrated.
 */
export * from "../services/execution/orderHandoff.js";
