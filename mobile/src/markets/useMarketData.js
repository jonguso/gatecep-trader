/**
 * Legacy compatibility wrapper.
 * Source moved to src/services/markets/useMarketData.js.
 * Keep this wrapper until Expo Router app imports are fully migrated.
 */
export { default } from "../services/markets/useMarketData.js";
export * from "../services/markets/useMarketData.js";
