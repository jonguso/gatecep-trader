/**
 * Legacy compatibility wrapper.
 * Source moved to src/services/trade/marketDepthData.js.
 * Keep this wrapper until Expo Router app imports are fully migrated.
 */
export * from "../services/trade/marketDepthData.js";
