/**
 * Legacy compatibility wrapper.
 * Source moved to src/services/version/versionRegistry.js.
 * Keep this wrapper until Expo Router app imports are fully migrated.
 */
export * from "../services/version/versionRegistry.js";
