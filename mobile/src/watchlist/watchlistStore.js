/**
 * Legacy compatibility wrapper.
 * Source moved to src/services/watchlist/watchlistStore.js.
 * Keep this wrapper until Expo Router app imports are fully migrated.
 */
export * from "../services/watchlist/watchlistStore.js";
