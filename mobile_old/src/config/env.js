// IMPORTANT:
// On a real phone, localhost points to the phone itself, not your laptop.
// Replace this IP with your laptop IPv4 address from `ipconfig`.

export const API_BASE_URL = "http://10.0.0.168:4000";
